import styled from "styled-components";

export const Container = styled.div`
    align-self: flex-end;
    display: flex;
    justify-content: center;
    gap: 5px;
    
    & svg {
        margin: 1rem 0;
     } 
`